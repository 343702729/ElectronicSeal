<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/appBg"
    android:orientation="vertical">
    <include layout="@layout/item_title"/>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="15dp"
        android:scrollbars="none">
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/white"
            android:paddingLeft="20dp"
            android:paddingRight="20dp"
            android:paddingTop="15dp"
            android:paddingBottom="15dp"
            android:orientation="vertical">
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="软件使用NFC读取设置权限需要打开，否则无法进行读取！"
                android:textSize="16sp"
                android:textColor="@color/grayDark"/>
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:text="2019-02-21"
                android:textSize="13sp"
                android:textColor="@color/darkLight"/>
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="10dp"
                android:text="软件使用NFC读取设置权限需要打开，否则无法进行读取！软件使用NFC读取设置权限需要打开，否则无法进行读取！软件使用NFC读取设置权限需要打开，否则无法进行读取！软件使用NFC读取设置权限需要打开，否则无法进行读取！软件使用NFC读取设置权限需要打开，否则无法进行读取！软件使用NFC读取设置权限需要打开，否则无法进行读取！"
                android:textSize="14sp"
                android:textColor="@color/darkLight"/>
        </LinearLayout>
    </ScrollView>
</LinearLayout>
